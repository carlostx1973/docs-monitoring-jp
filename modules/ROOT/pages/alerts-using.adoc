= アラートの使用
ifndef::env-site,env-github[]
include::_attributes.adoc[]
endif::[]

*https://www.mulesoft.com/anypoint-pricing[Titanium サブスクリプションが必要]*

カスタムダッシュボードのグラフのアラートをセットアップしたら、それらのアラートを有効化または無効化し、<<alert_status,アラート状況>>に基づいてアラートを絞り込み、そのアラートに関連付けられているカスタムダッシュボード内のグラフに移動できます。

[[alerts]]
image::alerts.png[アラートの例]

== アラート状況によるアラートの絞り込み

状況に基づいてアラートを絞り込むことができます。

image::alerts-filter.png[アラート状況による絞り込み]

[[alert_status]]
アラート状況で次のように絞り込まれます。

* *All states (すべての状態)*
* *No data (データなし)*: アラートしきい値を評価するためのデータが系列にありません。
* *Disabled (無効)*: アラートは無効化されています。
* *Alerting (アラート)*: メトリクスの値がしきい値を超えています。
* *OK*: メトリクスの値はしきい値未満です。
* *Pending (待機中)*: アラートは有効化されており、評価を待機中です。

== アラートの有効化と無効化

右側の<<alerts,スイッチ>>を切り替えることで、アラートを有効化または無効化できます。

* 無効化されているアラートを有効化すると、アラートは評価されるまで *[Pending (待機中)]* になります。値に応じて、アラートは *[OK]* または *[Alerting (アラート)]* に移行します。
* アラートを無効化すると、アラートは *[Disabled (無効)]* に移行します。
